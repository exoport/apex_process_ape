// app.js — inline-helper extraction point. Empty for the initial PLAN-5 / C8
// ship; HTMX OOB swaps + the stdlib SSE extension cover the locked event
// schema (pipeline-init / stage-* / hook / reply / await-* / stopped / error).
// Add small client-only widgets (copy-to-clipboard, modal toggles) here as
// they appear. Alpine adoption is explicitly deferred per PLAN-5 / C8.
